import { SectionLoading } from "@/components/Loading";

export default function ReportsLoading() {
  return (
    <SectionLoading title="Reports" subtitle="User-flagged sightings and comments (admin only)." />
  );
}
